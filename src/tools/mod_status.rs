use md5;
use std::fs;

pub fn check_if_modded(file_path:String, title:String) {
    println!("Under construction.");
    // hash the current file
    //let mut file = fs::File::open(&file_path)?;
    //let hash = md5::digest_

}
